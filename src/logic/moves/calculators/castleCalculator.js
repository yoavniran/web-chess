import getCanCastle from "logic/helpers/getCanCastle";

const castleCalculator = (square, symbol, state, pieceColor) => {

	if (getCanCastle(state.castles, pieceColor)) {

	}
};

export default castleCalculator;
